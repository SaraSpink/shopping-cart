<p id="notice"><%= notice %></p>
<div class="container">
  <div class="row">
    <div class="col-md-6">
      <h1>Products</h1>

      <% @products.each do |product| %>
       <p><%= link_to product.name, product_path(product), remote: true %> | <%= number_to_currency product.price %>
       <%= form_for @order_item, remote: true do |f| %>
         <%= f.hidden_field :product_id, value: product.id %>
         <%= f.number_field :quantity %>
         <%= f.submit "Add to cart" %>
       <% end %></p>
      <% end %>
      <br>

      <%= link_to 'New Product', new_product_path %>

    </div>
    <div class="col-md-6">
      <br>
      <br>
      <br>
      <span  id="render_total"></span>
      <br>
      <br>
      <br>
      <span  id="show_product_detail"></span>
    </div>
  </div>
</div>
